version: '3.8'

services:
  # Base de datos PostgreSQL
  postgres:
    image: postgres:15-alpine
    container_name: sistema-academico-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: sistema_academico
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    networks:
      - academico-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d sistema_academico"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Aplicación Spring Boot (opcional, para producción)
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: sistema-academico-app
  #   restart: unless-stopped
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     SPRING_PROFILES_ACTIVE: dev  # Usa perfil DEV (crea tablas automáticamente)
  #     SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/sistema_academico
  #     SPRING_DATASOURCE_USERNAME: admin
  #     SPRING_DATASOURCE_PASSWORD: admin123
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - academico-network

volumes:
  postgres_data:
    driver: local

networks:
  academico-network:
    driver: bridge
